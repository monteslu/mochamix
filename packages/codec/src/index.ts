/**
 * @internal-dj/codec — audio decode (and later encode).
 */

export {
  decodeArrayBuffer,
  fromAudioBuffer,
  decodeWithFfmpeg,
  isPlatformDecodable,
  PLATFORM_DECODABLE,
} from './decode.js';
