{
  "name": "@dj/codec",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Audio decode (and later encode). M1: decodeAudioData → planar Float32 SAB. ffmpeg-wasm worker path (from Loukai) for exotic formats, deferred. (05-library-and-data.md §4-5)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./recorder-worklet": "./src/recorder.worklet.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@dj/audio-engine": "*"
  }
}
