{
  "name": "@internal-dj/waveform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Waveform peak precompute + render. Max-abs peak buckets (Mixxx model, 05-library-and-data.md §7), two resolutions, rendered to canvas (Canvas2D now, WebGPU/WGSL next).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  }
}
