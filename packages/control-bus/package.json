{
  "name": "@dj/control-bus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The ControlObject bus — the spine. A named (group,key)->number store, mirrored to renderer (IPC) and to the audio worklet (SharedArrayBuffer). Mirrors Mixxx's ControlObject model.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./keys": "./src/keys.ts",
    "./sab": "./src/sab.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  }
}
